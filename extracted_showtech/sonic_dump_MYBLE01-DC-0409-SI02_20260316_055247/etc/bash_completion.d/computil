_computil_completion() {
    COMPREPLY=( $( env COMP_WORDS="${COMP_WORDS[*]}" \
                   COMP_CWORD=$COMP_CWORD \
                   _COMPUTIL_COMPLETE=complete $1 ) )
    return 0
}

complete -F _computil_completion -o default computil;
